services:
  - type: web
    name: mp-backend-api
    env: node
    buildCommand: "npm install -g pnpm && pnpm install --production=false && pnpm run db:generate && pnpm --filter @mdms/types build && pnpm --filter @mdms/api build"
    startCommand: "pnpm --filter @mdms/api start:prod"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false
